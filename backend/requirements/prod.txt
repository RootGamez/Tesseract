-r base.txt

gunicorn==22.0.0
uvicorn[standard]==0.30.1

# Sirve los estáticos de Django (admin/DRF) desde el propio proceso ASGI, sin
# necesidad de un servidor web aparte. El reverse proxy externo (NPM) sólo enruta.
whitenoise==6.7.0
